/**
 * @module @guess-who/shared
 *
 * Barrel export for the shared package.
 * Re-exports all types, character data, and message definitions
 * consumed by both the server and the web client.
 */

export * from "./types";
export * from "./characters";
export * from "./messages";
